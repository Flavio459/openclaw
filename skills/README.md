# Skills Layout

This repository now keeps only the core Collegium skill set in `skills/`.

Active in repo:
- `collegium-context`
- `surface-separation`
- `scope-classifier`
- `protocol-defensability`
- `forum-deliberation`

Archived skills:
- 52 skills moved to the Obsidian vault at `W:\Collegium Cortex\Skills Archive\OpenClaw Push`

Use the archive when a situational skill is needed, but keep the repository focused on the core set above.
